/**
 * O pacote main é o entry-point da aplicação. Nele está contida somente uma classe: {@link main.Main}, que é
 * responsável por carregar os dados iniciais e inicializar a interface.
 */
package main;