/**
 * O pacote view contém todas as classes que compôem a interface do programa. O papel das classes desse pacote é
 * somente mostrar os dados para o usuário, o controle do fluxo da aplicação é completa responsabilidade do pacote
 * {@link controller}.
 */
package view;