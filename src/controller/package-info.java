/**
 * O pacote controller contém as classes responsáveis por gerenciar a interação com o usuário, executando as ações
 * associadas a diversos elementos da interface, como botões, campos de textos e checkboxes.
 */
package controller;